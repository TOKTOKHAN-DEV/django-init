from app.common.schedule_registry import schedule
from app.common.views import ScheduleView


@schedule(name="{{ app_name }}", path="{{ app_name }}/", cron_expression=None)
class {{ camel_case_app_name }}Schedule(ScheduleView):
    def schedule(self):
        pass
