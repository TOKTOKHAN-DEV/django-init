AWSTemplateFormatVersion: '2010-09-09'

Description:
  'DDB IaC'

Parameters:
  ProjectName:
    Type: String
  ENV:
    Type: String
    AllowedValues:
      - dev
      - prod

Conditions:
  IsProd: !Equals [!Ref ENV, 'prod']

Resources:
  ConnectionTable:
    Type: AWS::DynamoDB::Table
    Properties:
      TableName: !Sub '${ProjectName}-${ENV}-connection-table'
      AttributeDefinitions:
        - AttributeName: connection_id
          AttributeType: S
        - AttributeName: user_id
          AttributeType: N
      KeySchema:
        - AttributeName: connection_id
          KeyType: HASH
      BillingMode: PAY_PER_REQUEST
      GlobalSecondaryIndexes:
        - IndexName: UserIdIndex
          KeySchema:
            - AttributeName: user_id
              KeyType: HASH
          Projection:
            ProjectionType: ALL

Outputs:
  ConnectionTable:
    Value: !Ref ConnectionTable
    Export:
      Name: !Sub '${ProjectName}-${ENV}-ConnectionTable'
  ConnectionTableArn:
    Value: !GetAtt ConnectionTable.Arn
    Export:
      Name: !Sub '${ProjectName}-${ENV}-ConnectionTableArn'